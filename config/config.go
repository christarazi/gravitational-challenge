package config

const (
	// Port is the port number which the server is listening on.
	// TODO: This is hard coded for now. In the future, we can have a
	// configurable address / port number.
	Port = 8080
)
